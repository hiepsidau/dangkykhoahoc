<%=form_for :student, url: sign_up_students_path, method: :post do |f|%>
	<%= f.label :email %>
	<%= f.email_field :email%>
	<%= f.label :password %>
	<%= f.password_field :password%>
	<%= f.submit%>
<% end %>